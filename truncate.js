/*
Write a function that truncates a string (first argument) 
if it is longer than the given maximum string length (second argument) 
as a given input. It should return a single, truncated string. 
However, be sure that the last 3 characters of the returned 
string are an ellipsis (...).
*/ 

// E.g. truncate("This is a string", 8); -> "This ..."

function truncate(str, num) {
    
}